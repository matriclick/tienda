<div class="field" style="margin-top:-10px;">
	<%= f.fields_for :dress_images do |di_form| %>
		<%= di_form.hidden_field :id %>
		<%= image_tag di_form.object.dress.url(:tiny) %>
		<%= di_form.file_field :dress %>
		<%= di_form.link_to_remove t('remove_this_image'), :class => 'small_button' %>
	<% end %>
	<br/>
	<br/>
	<%= f.link_to_add t('add_image'), :dress_images, :class => 'btn btn-primary' %>
</div>
<div class="help_tip" style="font-weight:lighter; font-size:11px; margin:5px 0px 10px 0px;">
	- <b>950px x 1400px</b> es el tamaño ideal para las imágenes<br />
	- Siempre preferir <b>fondo blanco</b><br />
	- Cada imagen por sí sola NO DEBE SUPERAR los <b>2MB</b><br />
	- La primera imagen que se sube será la principal<br />
	- No subir más de 3 imágenes a la vez<br />
</div>