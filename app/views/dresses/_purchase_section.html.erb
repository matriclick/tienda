<% if !dress.supplier_account.supplier_account_type.nil? %>
	<% if dress.has_stock? %>
		<%= link_to "AGREGAR AL CARRITO", 
					buy_add_to_cart_path(:purchasable_type => dress.class, :purchasable_id => dress.id), 
					:method => :post, :class => 'btn btn-primary', :style => 'width:400px' %>
	<% else %>
		ITEM SIN STOCK DISPONIBLE<br />
	<% end %>
<% end %>