<div class="row">
	<div class="col-md-10">
		<div class="row">
		<% if defined? dresses and dresses.size > 0 %>
		  <% dresses.each do |dress| %>
				<%= render 'dresses/list_dress', dress: dress %>
		  <% end %>
		<% else %>
			<%= render 'layouts/not_found' %>
		<% end %>
		</div>
		<% unless @not_paginate %>
		<div class="row" style="text-align:center">
		 <%= render 'dresses/pagination', dresses: dresses %>
		</div>
		<% end %>
	</div>
	<div class="col-md-2">
		<%= render 'dresses/right_menu', sizes: @sizes %>
	</div>
</div><!--/row-->